# BQ-Microservices
Microservices for Beautinique

## Run all services from root

1. Install dependencies at the repository root:

```bash
npm install
```

2. Run every microservice found under the root folders:

```bash
npm run dev:all
```

3. To start all services in production mode (build + run), use:

```bash
npm run start:all
```
